<!-- Common Head -->
${head}
<body>
${authbar}

<div id="profile">
    <div class="container">
        <div class="span3">
            ${profile_picture}
        </div>
        <div class="span5">
            <h1>${name}</h1>
            <p>${gender}</p>
            <p>${degree}</p>
            <p>${student_number}</p>
        </div>
        <div class="span3">
            ${controls}
        </div>
        <div class="span9">
            <h3>About Me</h3>
            <p>${about}</p>
        </div>
    </div>
</div>

<div>
    <div class="container">
        <div class="section">
            <br />
            <ul class="nav nav-tabs">
                <li><a href="#mates" data-toggle="tab">Mates</a></li>
                <li><a href="#news" data-toggle="tab">News</a></li>
                <li><a href="#gallery" data-toggle="tab">Gallery</a></li>
                ${edit_pane_tab}
                ${upload_pane_tab}
            </ul>
            <div id="myTabContent" class="tab-content">
                <div class="tab-pane fade in active" id="mates">
                    ${matelist}
                </div>
                <div class="tab-pane fade" id="news">
                </div>
                <div class="tab-pane fade" id="gallery">
                    ${gallery}
                </div>
                <div class="tab-pane fade" id="edit">
                    ${edit_pane_contents}
                </div>
                <div class="tab-pane fade" id="upload">
                    ${upload_pane_contents}
                </div>
            </div>
        </div>
    </div>
</div>
